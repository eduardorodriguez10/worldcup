<ul class="nav">
  <li>
    <%= link_to "World Cup Bracket!", root_path, :class => 'brand' %>
  </li>
  <li>
    <%= link_to 'Your Bracket', bracket_path(current_user.slug) if logged_in? %>
  </li>
  <li>
  	<%= link_to 'Groups', groups_path if logged_in? %>
  </li>
  <% if !logged_in? %>
      <li class="right">
        <%= link_to 'Login', login_path %>
      </li>
      <li class="right">
      	<%= link_to 'Register', register_path %>
      </li>
  <% end %>
     <div class="btn-group nav_item right">
      <% if logged_in? %>
        <a class="btn btn-small dropdown-toggle" data-toggle="dropdown" href="#"><span class="caret"></span> <%= current_user.first_name %>'s Account</a>
        <ul class="dropdown-menu">
          <li>
            <%= link_to 'Edit Profile', edit_user_path(current_user.slug) %>
          </li>
          <li>
            <%= link_to 'Logout', logout_path %>
          </li>
        </ul>
      <% end %>
    </div>
</ul>
