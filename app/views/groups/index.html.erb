<%= render 'group_buttons' %>
<div class="col-md-12">
	<%if @groups.empty? %>
		<h4>You haven't joined any groups yet.</h4>
	<% else %>
			<div class="well">
				<%= render 'group_table' %>
			</div>
	<% end %>
</div>